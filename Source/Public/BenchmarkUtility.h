#pragma once

namespace BenchmarkUtility
{
	//double RunSingleAllocationTest(Allocator* allocator, const std::size_t object_size);
	//double RunMultipleAllocationTest(Allocator* allocator, const std::size_t object_size, const std::size_t count);
	//double RunSingleDeallocationTest(Allocator* allocator, const std::size_t object_size);
	//double RunMultipleDeallocationTest(Allocator* allocator, const std::size_t object_size, const std::size_t count);
}

